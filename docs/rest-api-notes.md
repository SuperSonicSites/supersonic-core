# REST API Notes

REST automation can be useful, but live writes are controlled.

## Rules

- Do not run live REST writes without approval.
- Generate and validate payloads first.
- Use dry-run mode before live writes.
- Review changed pages after writing.
- Capture screenshots when REST changes affect visible content.

## V1 Status

No REST scripts are implemented in Phase 1.

