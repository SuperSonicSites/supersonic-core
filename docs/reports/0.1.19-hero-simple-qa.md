# 0.1.19 Hero: Simple QA Report

**Date:** 2026-06-01
**Scope:** `supersonic-site-theme/hero-simple` only
**Theme version:** `0.1.19`
**Plugin version:** `0.1.8`
**Staging:** `https://staging.yukonrealestateconnection.ca/`
**QA page:** `QA - Pattern - hero-simple`
**QA page cleanup:** pages `93`, `95`, and `96` trashed after capture/checks

---

## 1. Scope Reviewed

This review checked the `Hero: Simple` justification, gutter, and responsive layout contract after the 0.1.15 and 0.1.16 failures.

Latest prior report cross-checks:

- `docs/reports/0.1.15-hero-simple-qa.md`
- `docs/reports/0.1.16-hero-simple-qa.md`

---

## 2. Active Versions On Staging

| Component | Version / status |
| --- | --- |
| WordPress core | 7.0 |
| Theme | `supersonic-site-theme` 0.1.19 |
| Plugin | `supersonic-site-core` 0.1.8 |
| Registered patterns | 26 |
| REST status | HTTP 200 (`rest:check`, `certify:staging`) |

---

## 3. Method

- Used the repo `visual-qa` skill.
- Ran `npm run validate`.
- Ran `npm run pattern:registry:check`.
- Ran `npm run certify:staging -- 0.1.19 0.1.8`.
- Confirmed no pre-existing QA pages with `npm run rest:qa-pages`.
- Created a temporary QA page for `supersonic-site-theme/hero-simple`.
- Captured desktop, tablet, and mobile screenshots with a cache-busted URL.
- Created temporary center and right variants from the same block markup to verify the Gutenberg `justifyContent` states.
- Inspected rendered frontend markup and computed layout with Playwright.
- Ran cleanup dry-runs and trashed all temporary QA pages.

Screenshots captured:

- `screenshots/after/0.1.19-hero-simple-qa/hero-simple-desktop.png`
- `screenshots/after/0.1.19-hero-simple-qa/hero-simple-tablet.png`
- `screenshots/after/0.1.19-hero-simple-qa/hero-simple-mobile.png`

---

## 4. Findings

No blocking visual or layout issues were found in the 0.1.19 pass.

### Verified

| Contract | Result | Evidence |
| --- | --- | --- |
| Outer layout | PASS | On desktop, the hero group renders at `x=0`, width `1440`, with `72px` left/right padding. |
| Text rail | PASS | The readable rail is `760px`, with no nested interior padding group. |
| Justification left | PASS | The H1 starts at `x=72` on a 1440px viewport. |
| Justification center | PASS | A temporary center variant moved the H1 rail to `x=340`. |
| Justification right | PASS | A temporary right variant moved the H1 rail to `x=608`. |
| Desktop/tablet/mobile screenshots | PASS | All three breakpoints are readable, without horizontal overflow or text clipping. |
| QA page cleanup | PASS | Temporary QA pages `93`, `95`, and `96` were trashed; `rest:qa-pages` returned `[]`. |

### Remaining Non-Blocking Content Item

| # | Pattern | Finding | Recommendation |
| --- | --- | --- | --- |
| M1 | hero-simple | Starter CTA links still use `href="#"`. | Replace with real destinations during site build/content pass. |

---

## 5. Approval Status

**PASS** - `Hero: Simple` is approved for the tested 0.1.19 behavior. Manual editor verification is still recommended as a final human sanity check, but the rendered block contract now proves left, center, and right positioning work through native Gutenberg markup.
